# 玻璃期货 AI 择时 —— 依赖
# 核心框架
vnpy>=4.4.0            # 自带 vnpy.alpha（AlphaLab / AlphaDataset / LgbModel / 回测引擎）
lightgbm>=4.6.0       # 梯度提升树模型
scikit-learn>=1.5.0   # 依赖工具
scipy>=1.13.0         # RankIC (spearmanr)

# 数据处理
polars>=1.0.0
pandas>=2.2.0
numpy>=1.26.0
pyarrow>=15.0.0       # 读写 parquet

# vnpy.alpha 因子/评估依赖
alphalens-reloaded    # show_signal_performance 用（可选）
tqdm
matplotlib
